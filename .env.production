# Production gọi thẳng BE "Gamma Trade Platform" (FastAPI).
# Cập nhật 2026-06-05 (Tuấn): BE thật, dùng được (tạo bot vào DB + có data
# backtest) là tradingbot.ne.com:8088. Host ai-gamma staging cũ đã stale
# (bot tạo xong không vào DB thật).
# ⚠️ Host này là HTTP → FE prod chạy HTTPS sẽ bị mixed-content block.
#   Cần BE bật HTTPS, hoặc Vercel rewrite/proxy, hoặc deploy FE qua HTTP.
# Prod deploy: BE cần allow CORS origin của domain FE.
VITE_API_BASE_URL=http://tradingbot.ne.com:8088
