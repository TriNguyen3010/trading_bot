# Production gọi thẳng BE "Gamma Trade Platform" (FastAPI).
# BE team confirmed 2026-05-29: server thật là host HTTPS staging dưới đây
# (tradingbot.ne.com chỉ là UI, không phải server). HTTPS → hết mixed-content.
# Prod deploy: BE cần allow CORS origin của domain FE.
VITE_API_BASE_URL=https://ai-gamma-trade-stg.coin98.dev
